//import 'package:langchain/langchain.dart';

//enum BookType { history, science }

///This function is used to read books of different topics.
///If the user wants to learn info about any topic in "İnkilap Tarihi ve Atatürkçülük" lesson use [BookType.history]
///If the user wants to learn info about any topic in "Fen Bilimleri" lesson use [BookType.science]
/*
Future<String> readBook(BookType bookType) async {
  if (bookType == BookType.history) {
    return await TextLoader(filePath).load();
  }
}
*/